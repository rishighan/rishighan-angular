language: node_js

node_js:
  - 0.10.33

on_success:
  - if [ "$BRANCH" == "master" ]; then git remote add dokku dokku@165.227.101.66:rg-dokku-renuka; fi
  - if [ "$BRANCH" == "master" ]; then git push dokku master; fi
