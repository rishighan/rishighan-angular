# This references the default nodejs container from
# the Docker Hub: https://registry.hub.docker.com/_/node/
# If you want Nodesource's container you would reference nodesource/node
# Read more about containers on our dev center
# http://devcenter.wercker.com/docs/containers/index.html
box: node
# This is the build pipeline. Pipelines are the core of wercker
# Read more about pipelines on our dev center
# http://devcenter.wercker.com/docs/pipelines/index.html

# You can also use services such as databases. Read more on our dev center:
# http://devcenter.wercker.com/docs/services/index.html
# services:
    # - postgres
    # http://devcenter.wercker.com/docs/services/postgresql.html

    # - mongo
    # http://devcenter.wercker.com/docs/services/mongodb.html
build:
  # The steps that will be executed on build
  # Steps make up the actions in your pipeline
  # Read more about steps on our dev center:
  # http://devcenter.wercker.com/docs/steps/index.html
  steps:
    # A step that executes `npm install` command
    - npm-install
    - script:
        name: bower install
        code: |
          npm i -g bower
          bower i --config.interactive=false --allow-root
    # A step that executes `npm test` command
    - npm-test

    # A custom script step, name value is used in the UI
    # and the code value contains the command that get executed
    - script:
        name: echo nodejs information
        code: |
          echo "node version $(node -v) running"
          echo "npm version $(npm -v) running"

deploy:
  steps:
    - npm-install
    - script:
        name: "install supervisor"
        code: "npm i -g supervisor"
    - internal/docker-push:
      username: $DOCKER_USERNAME
      password: $DOCKER_PASSWORD
      repository: frishi/rishighanangular_web
      ports: "8080"
      cmd: "/bin/bash -c \"cd /pipeline/source && npm start\""

    - add-ssh-key:
        keyname: DIGITALOCEAN
    - add-to-known_hosts:
        hostname: 165.227.124.215
    - script:
        name: pull latest image
        code: ssh root@165.227.124.215 docker pull frishi/rishighanangular_web:latest
    - script:
        name: stop running container
        code: ssh root@165.227.124.215 docker stop rgapp || echo "failed to stop running container"
    - script:
        name: remove stopped container
        code: ssh root@165.227.124.215 docker rm rgapp || echo "failed to remove stopped container"
    - script:
        name: remove image behind stopped container
        code: ssh root@165.227.124.215 docker rmi -f frishi/rishighanangular_web:latest || echo ‘failed to remove image behind stopped container’
    - script:
        name: run new container
        code: ssh root@165.227.124.215 docker run -d -p 80:8080 --name rgapp frishi/rishighanangular_web:latest

